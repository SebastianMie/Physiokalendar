package com.example.physiokalendar.entity;

/**
 * Enum for audit event entity types.
 */
public enum AuditEntityType {
    THERAPIST,
    PATIENT,
    APPOINTMENT,
    APPOINTMENT_SERIES,
    ABSENCE,
    USER,
    SYSTEM
}
