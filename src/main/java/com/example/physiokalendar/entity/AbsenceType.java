package com.example.physiokalendar.entity;

/**
 * Enum for absence types.
 */
public enum AbsenceType {
    /** One-time absence on a specific date */
    SPECIAL,
    /** Recurring absence on specific weekday(s) */
    RECURRING
}
